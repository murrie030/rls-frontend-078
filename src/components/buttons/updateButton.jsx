import React from "react";

const UpdateButton = () => {
  const button = {
    color: "#ffffff",
  };
  return (
    <button type="button" className="btn btn-warning btn-sm" style={button}>
      Update
    </button>
  );
};

export default UpdateButton;
